# CLI Commands

| Command | Description |
| --- | --- |
| `poetry run release_guard` | Release safety checks prior to deploy. |
| `poetry run pytest-safe` | Pytest wrapper with safe defaults. |
| `python -m signal_client.compatibility --strict` | Validates REST compatibility. |

Expand this table (or migrate to table-reader) as scripts grow.
